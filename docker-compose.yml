version: '3.8'

services:
  toniai-bot:
    build: .
    container_name: toniai_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # This saves your chat logs safely on the host machine
      - ./chats:/app/chats